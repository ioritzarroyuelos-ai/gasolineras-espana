{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "icons": "node scripts/gen-icons.mjs",
    "features": "node scripts/gen-features-js.mjs",
    "sw": "node scripts/gen-sw.mjs",
    "prebuild": "node scripts/gen-icons.mjs && node scripts/gen-features-js.mjs && node scripts/gen-sw.mjs",
    "build": "vite build",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium",
    "lh": "lhci autorun",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "backfill:d1": "node scripts/backfill-d1.mjs",
    "ship": "node scripts/ship.mjs",
    "postinstall": "git rev-parse --git-dir >/dev/null 2>&1 && git config --local core.hooksPath .githooks || true"
  },
  "dependencies": {
    "hono": "^4.12.15",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.2",
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.25.3",
    "@lhci/cli": "^0.15.1",
    "@napi-rs/canvas": "^0.1.100",
    "@playwright/test": "^1.59.1",
    "@resvg/resvg-js": "^2.6.2",
    "pdf-parse": "^2.4.5",
    "tesseract.js": "^7.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vitest": "^4.1.5",
    "wrangler": "^4.85.0"
  },
  "overrides": {
    "@hono/node-server": ">=1.19.13"
  }
}
